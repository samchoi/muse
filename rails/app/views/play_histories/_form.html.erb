<%= form_for(@play_history) do |f| %>
  <% if @play_history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@play_history.errors.count, "error") %> prohibited this play_history from being saved:</h2>

      <ul>
      <% @play_history.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :audio_id %><br>
    <%= f.number_field :audio_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :data %><br>
    <%= f.text_field :data %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
